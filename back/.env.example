PORT=8080
# Upstash Redis (production). Leave empty for local dev (in-memory sessions).
REDIS_URL=
REDIS_PASSWORD=
# Comma-separated browser origins allowed to open WebSockets, e.g.:
# ALLOWED_ORIGINS=https://mahajoni.vercel.app
ALLOWED_ORIGINS=
